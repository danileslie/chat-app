{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "This application allows users to communicate through the use of one-to-one chats.",
  "main": "index.js",
  "scripts": {
    "build": "npm i --prefix server && npm i --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
